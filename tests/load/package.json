{
  "name": "@mother-harness/load-tests",
  "version": "0.1.0",
  "description": "Load testing suite for Mother-Harness",
  "type": "module",
  "private": true,
  "scripts": {
    "test:load": "artillery run scenario-concurrent-tasks.yml",
    "test:stress": "artillery run scenario-stress.yml",
    "test:quick": "artillery quick --count 10 --num 50 http://localhost:8000/health",
    "report": "artillery report"
  },
  "devDependencies": {
    "artillery": "^2.0.0",
    "artillery-plugin-metrics-by-endpoint": "^1.5.0"
  }
}
